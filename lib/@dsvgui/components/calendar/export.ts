import { Base, Compact, WithStreak } from "./index.stories";
import { Calendar } from "./index";
import { getGridComponents } from "../../utils";

export default getGridComponents([Base, Compact, WithStreak], Calendar);
