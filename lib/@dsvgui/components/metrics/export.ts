import { getGridComponents } from "../../utils";
import { Metrics } from "./index";

import { Base, Multiple, Long } from "./index.stories";

export default getGridComponents([Base, Multiple, Long], Metrics);
